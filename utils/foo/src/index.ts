//import "../node_modules/ts-jest"

// no auto-import suggestions for this, _unless_ you uncomment the 
// import statement above (or any similar import statement for
// a package, e.g., import "../node_modules/typescript" will work too)
useEffect()

export function myFunction() {
  const foo = "hello, world"
  return foo
}